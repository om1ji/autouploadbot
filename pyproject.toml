[project]
name = "autouploadbot"
version = "3.0.0"
description = "Serverless bot that reposts new YouTube uploads as audio to a Telegram channel"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.24.0",
    "boto3>=1.43.98",
    "yt-dlp[default]>=2026.8.19",
]
