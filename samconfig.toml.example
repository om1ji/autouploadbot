# Copy to samconfig.toml and fill in your values:
#   cp samconfig.toml.example samconfig.toml
# samconfig.toml is git-ignored: it ends up holding account-specific data
# (ECR repository URIs with your AWS account ID).
version = 0.1

[default.deploy.parameters]
stack_name = "autouploadbot"
resolve_s3 = true
resolve_image_repos = true
s3_prefix = "autouploadbot"
region = "us-west-1"
confirm_changeset = true
capabilities = "CAPABILITY_IAM"
# AdminChatId: your Telegram user ID — alerts go there. Send /start to the bot,
#   then read it from https://api.telegram.org/bot<token>/getUpdates
# The channel map (ChannelMap) comes from channels.yaml — deploy with
# python3 tools/deploy.py, which merges it with these overrides.
parameter_overrides = "AdminChatId=\"123456789\""

[default.global.parameters]
region = "us-west-1"
